# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Cloudless' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Cloudless
  pod 'Firebase'
  pod 'Firebase/Core'
  pod 'FirebaseAuth'
  pod 'FirebaseDatabase'
  pod 'FirebaseFirestore'

  target 'CloudlessTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CloudlessUITests' do
    # Pods for testing
  end

end
